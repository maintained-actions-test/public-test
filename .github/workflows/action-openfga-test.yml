name: action-openfga-test

on:
  workflow_dispatch:

jobs:
  openfga-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: step-security/action-openfga-test@release
        with:
          test_path: example
          test_files_pattern: "*.fga.yaml"
