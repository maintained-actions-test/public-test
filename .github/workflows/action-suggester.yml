name: action-suggester
on: 
  workflow_dispatch:

jobs:
    suggest:
        runs-on: ubuntu-latest
        steps:  
            - uses: actions/checkout@v6
            - name: suggest
              uses: step-security/action-suggester@main
