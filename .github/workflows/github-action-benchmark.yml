name: github-action-benchmark

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  github-action-benchmark:
    name: github-action-benchmark
    runs-on: ubuntu-latest
    steps:
      - uses: step-security/harden-runner@rc
        with:
          egress-policy: audit

      - name: Checkout code
        uses: actions/checkout@v6

      - name: Run github-action-benchmark
        uses: step-security/github-action-benchmark@main
        with:
          tool: customBiggerIsBetter
          output-file-path: testdata/github-action-benchmark/output.json
          external-data-json-path: testdata/github-action-benchmark/data.json
          summary-always: true
          github-token: ${{ github.token }}
