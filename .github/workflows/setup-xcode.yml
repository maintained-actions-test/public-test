name: setup-xcode

on: 
  workflow_dispatch:


jobs:
  setup-xcode:
    runs-on: macos-latest
    steps:

      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Xcode
        uses: step-security/setup-xcode@main
        with: 
            xcode-version: latest-stable
