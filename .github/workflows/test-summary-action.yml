name: test-summary-action
on: 
  workflow_dispatch:

jobs:
    test-summary:
        runs-on: ubuntu-latest
        steps:
            - name: Checkout repo
              uses: actions/checkout@v4
            - name: create-test-summary
              uses: step-security/test-summary-action@main
              with: 
                paths: "./tests.xml"
